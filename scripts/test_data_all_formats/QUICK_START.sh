#!/bin/bash
################################################################################
# 快速测试 - 一键运行
# 自动检测当前目录，无需手动输入路径
# 使用简化版脚本，更稳定可靠
################################################################################

# 颜色
GREEN='\033[0;32m'
BLUE='\033[0;34m'
YELLOW='\033[1;33m'
NC='\033[0m'

echo -e "${BLUE}========================================================================${NC}"
echo -e "${BLUE}scRNA-DataHub 快速测试（自动模式 - 简化版）${NC}"
echo -e "${BLUE}========================================================================${NC}\n"

# 自动获取当前目录
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
TEST_DATA_DIR="$SCRIPT_DIR"

echo -e "${GREEN}✓ 测试数据目录: $TEST_DATA_DIR${NC}"
echo -e "${YELLOW}ℹ 使用简化版脚本（更稳定，保证完整运行）${NC}"
echo ""

# 使用echo传递路径给简化版测试脚本
echo "$TEST_DATA_DIR" | bash "$SCRIPT_DIR/run_all_format_tests_simple.sh"

